import React from "react";

const LandedOnTax = (props) => {
  return (
    <div>
        Landed On Tax
        <button onClick={props.buy}>   
        OK
    </button>
        
    </div>
  );
};

export default LandedOnTax;